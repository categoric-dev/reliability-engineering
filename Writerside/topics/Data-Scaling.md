# CAP Theorem

The CAP theorem is a fundamental concept for distributed systems, stating that it's impossible to simultaneously guarantee all three properties: Consistency (every read receives the most recent write), Availability (every request gets a response, though not necessarily the most recent data), and Partition Tolerance (the system continues to operate despite network disruptions). 